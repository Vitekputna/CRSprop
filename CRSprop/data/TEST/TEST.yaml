name: TEST

data:
  density:
    type: external
    data:
      path: "Density.yaml"
      
  thermalconductivity:
    type: external
    data:
      path: "Thermal_conductivity.yaml"

  viscosity:
    type: external
    data:
      path: "Viscosity.yaml"

  cp:
    type: external
    data:
      path : "cp.yaml"

  enthalpy:
    type: external
    data:
      path : "Enthalpy.yaml"

  entropy:
    type: external
    data:
      path : "Entropy.yaml"

  phase:
    type: external
    data:
      path : "Phase.yaml"

  vapor_pressure:
    type: external
    data:
      path: "Saturated.yaml"